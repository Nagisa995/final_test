import { ICookieAuthorizationInfo } from '../types/interface';

export const POSTERBASEURL: string = 'https://image.tmdb.org/t/p/w500';

export const cookieAuthorization: ICookieAuthorizationInfo = {
  cookieName: 'authorization',
  cookieValue: 'pass',
};

export const DEFAULTGENREFILTER: Array<number> = [];
